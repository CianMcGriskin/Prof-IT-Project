import NavigationBar from '../Navbar/Navbar'
const TimetablePage = () => {
    return (
        <div>
        <NavigationBar/>
        Timetable
        </div>
      );
};

export default TimetablePage;